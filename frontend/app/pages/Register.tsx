import AuthForm from "~/components/Forms/AuthForm";

const Register = () => {
    return (
        <AuthForm type="register" className="opacity-80 h-[60dvh] my-10"/>
    );
};

export default Register;
