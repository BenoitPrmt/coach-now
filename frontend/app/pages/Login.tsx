import AuthForm from "~/components/Forms/AuthForm";

const Login = () => {
    return (
        <AuthForm type="login" className="opacity-80 h-full min-h-[60dvh] my-10"/>
    );
};

export default Login;
